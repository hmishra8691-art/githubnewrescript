"use client";
/**
 * Studio authoring for the allocation family — see docs/VARIANT-BATCH.md §4.
 *
 * Nothing to register. Slider Allocation is authored through the `sum`
 * capability the family already declares — the sum target and unit are the
 * slider's own maximum and its readout — plus the ordinary option list.
 */
export {};
