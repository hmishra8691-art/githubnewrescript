"use client";
/**
 * Studio authoring for the text family — see docs/VARIANT-BATCH.md §4.
 *
 * Nothing to register. Rich Text is authored entirely through the controls the
 * text family already has: the placeholder, and min_length / max_length in the
 * validation editor — which measure the respondent's TEXT, not the markup
 * around it (see the length block in engine/validate.ts).
 */
export {};
