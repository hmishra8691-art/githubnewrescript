"use client";
/**
 * Studio authoring for the text family — see docs/VARIANT-BATCH.md §4.
 * Use registerVariantSettings("<renderer>", block) and
 * registerOptionMetaFields("<renderer>", [...]) from "./registry".
 */
export {};
