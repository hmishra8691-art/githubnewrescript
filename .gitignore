node_modules/
.next/
# the production build `scripts/p0-cookie-test.mjs` makes for itself, kept out
# of `.next` so it cannot overwrite a running dev server's chunks
.next-p0/
.next-blockers/
dist/
*.tsbuildinfo
.env
.env.local
.env*.local
.vercel/
.DS_Store
coverage/
*.log

# R2 credentials for scripts/r2-check.mjs — never committed
.env.r2
